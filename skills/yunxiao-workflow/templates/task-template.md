<!--
模板类型: 任务描述
用途: 作为 workitem create --category Task --desc-file <file> 的内容
标题规范: [父需求摘要] 具体模块的开发或任务  →  传入 --subject
子任务标题: [父任务摘要] 具体步骤或动作
示例标题: [用户画像标签] UserCenter 后端接口开发
规则:
  - 正文 200 字以内
  - 子任务也必须遵照本模板输出完整实施步骤与验证标准，禁止一句话敷衍
  - 填充所有 {{}} 占位符后，删除占位符标记和本注释块
  - 可选章节不适用时整节删除
-->

## 关联需求

<!-- 说明该任务从属于哪个业务需求 -->

关联工作项：#{{父需求 ID}}

## 实施步骤

<!-- 尽可能详细地列出实现步骤，避免模糊描述 -->

1. {{步骤 1：修改哪个目录/模块、实现什么}}
2. {{步骤 2：具体操作}}
3. {{步骤 3：具体操作}}

## 涉及模块与前置依赖（可选）

<!-- 仅在有前置依赖或跨模块影响时保留 -->

**前置依赖**：{{本任务开始前需完成的事项}}

**影响范围**：{{修改可能影响的其他模块}}

## 自测验证

<!-- 如何验证此任务已完成 -->

- [ ] {{验证条件 1：如单测覆盖率、接口测试}}
- [ ] {{验证条件 2：如功能交互验证}}

## 流程 / 架构图（可选）

<!-- 仅在涉及复杂时序调用、状态机或逻辑分支时保留，使用 mermaid 代码块 -->

```mermaid
{{图表内容}}
```
